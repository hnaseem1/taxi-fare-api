<p>hi <%= @user.first_name.capitalize %></p>

<% if @user_rides %> 
	<% @user_rides.each do |ride| %> 
		<ul>
			<li>From: <%= ride.start_address %></li>
			<li>To: <%= ride.end_address %></li>
			<li>Date: <%= ride.created_at.strftime("%Y-%m-%d") %></li>
		</ul>
	<% end %> 
<% end %> 